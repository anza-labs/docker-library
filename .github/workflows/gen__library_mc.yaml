jobs:
  build:
    env: {}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          password: ${{ secrets.GITHUB_TOKEN }}
          registry: ghcr.io
          username: shanduur-auto
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - run: |-
          docker \
              buildx \
              build \
              --load \
              --target=${{ matrix.target }} \
              --build-arg=VERSION=${{ matrix.version }} \
              --platform=linux/${{ matrix.arch }} \
              --file=library/mc/Dockerfile \
              --tag=ghcr.io/anza-labs/library/mc:${{ matrix.version }}-${{ matrix.arch }} \
              library/mc
      - run: |-
          docker \
              push \
              ghcr.io/anza-labs/library/mc:${{ matrix.version }}-${{ matrix.arch }}
    strategy:
      matrix:
        arch:
          - amd64
          - arm64
          - ppc64le
        target:
          - image
        version:
          - RELEASE.2025-07-21T05-28-08Z
  promote:
    env: {}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          password: ${{ secrets.GITHUB_TOKEN }}
          registry: ghcr.io
          username: shanduur-auto
      - uses: docker/setup-buildx-action@v3
      - run: |-
          docker \
              manifest \
              create \
              --amend \
              ghcr.io/anza-labs/library/mc:${{ matrix.version }} \
              ghcr.io/anza-labs/library/mc:${{ matrix.version }}-amd64 \
              ghcr.io/anza-labs/library/mc:${{ matrix.version }}-arm64 \
              ghcr.io/anza-labs/library/mc:${{ matrix.version }}-ppc64le
      - run: |-
          docker \
              manifest \
              annotate \
              --arch=amd64 \
              ghcr.io/anza-labs/library/mc:${{ matrix.version }} \
              ghcr.io/anza-labs/library/mc:${{ matrix.version }}-amd64 && \
          docker \
              manifest \
              annotate \
              --arch=arm64 \
              ghcr.io/anza-labs/library/mc:${{ matrix.version }} \
              ghcr.io/anza-labs/library/mc:${{ matrix.version }}-arm64 && \
          docker \
              manifest \
              annotate \
              --arch=ppc64le \
              ghcr.io/anza-labs/library/mc:${{ matrix.version }} \
              ghcr.io/anza-labs/library/mc:${{ matrix.version }}-ppc64le
      - run: |-
          docker \
              manifest \
              push \
              ghcr.io/anza-labs/library/mc:${{ matrix.version }}
    strategy:
      matrix:
        target:
          - image
        version:
          - RELEASE.2025-07-21T05-28-08Z
name: release library/mc
"on":
  push:
    branches:
      - main
    paths:
      - .github/workflows/gen__library_mc.yaml
      - library/mc/Dockerfile
  workflow_dispatch: {}
permissions:
  contents: write
  packages: write
