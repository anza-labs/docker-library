jobs:
  build:
    env: {}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f
        with:
          password: ${{ secrets.GITHUB_TOKEN }}
          registry: ghcr.io
          username: shanduur-auto
      - uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8
      - uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c
      - run: |-
          docker \
              buildx \
              build \
              --load \
              --label=org.opencontainers.image.authors='anza-labs contributors' \
              --label=org.opencontainers.image.created='${{ github.event.created_at }}' \
              --label=org.opencontainers.image.revision='${{ github.sha }}' \
              --label=org.opencontainers.image.source='https://github.com/anza-labs/docker-library' \
              --label=org.opencontainers.image.url='${{ github.event.repository.html_url }}' \
              --label=org.opencontainers.image.vendor='anza-labs' \
              --target=${{ matrix.target }} \
              --build-arg=VERSION=${{ matrix.version }} \
              --build-arg=BASE_IMAGE=gcr.io/distroless/static:latest@sha256:47b2d72ff90843eb8a768b5c2f89b40741843b639d065b9b937b07cd59b479c6 \
              --platform=linux/${{ matrix.arch }} \
              --file=library/go-busybox/Dockerfile \
              --tag=ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }}-${{ matrix.arch }} \
              library/go-busybox
      - run: |-
          docker \
              push \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }}-${{ matrix.arch }}
      - run: curl -sL "https://github.com/google/go-containerregistry/releases/latest/download/go-containerregistry_$(uname -s)_$(uname -m).tar.gz" | tar xz -C /usr/local/bin crane
      - uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6
      - if: ${{ matrix.version != 'latest' }}
        run: |-
          cosign \
              sign \
              --yes \
              ghcr.io/anza-labs/library/go-busybox@$(crane digest ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }}-${{ matrix.arch }})
    strategy:
      matrix:
        arch:
          - amd64
          - arm64
          - ppc64le
          - riscv64
        target:
          - image
        version:
          - 0158b4566f6b5ea07d014d8f2b579458f213ae6b
  promote:
    env: {}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f
        with:
          password: ${{ secrets.GITHUB_TOKEN }}
          registry: ghcr.io
          username: shanduur-auto
      - uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c
      - run: |-
          docker \
              manifest \
              create \
              --amend \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }} \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }}-amd64 \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }}-arm64 \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }}-ppc64le \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }}-riscv64
      - run: |-
          docker \
              manifest \
              annotate \
              --arch=amd64 \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }} \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }}-amd64 && \
          docker \
              manifest \
              annotate \
              --arch=arm64 \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }} \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }}-arm64 && \
          docker \
              manifest \
              annotate \
              --arch=ppc64le \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }} \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }}-ppc64le && \
          docker \
              manifest \
              annotate \
              --arch=riscv64 \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }} \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }}-riscv64
      - run: |-
          docker \
              manifest \
              push \
              ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }}
      - run: curl -sL "https://github.com/google/go-containerregistry/releases/latest/download/go-containerregistry_$(uname -s)_$(uname -m).tar.gz" | tar xz -C /usr/local/bin crane
      - uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6
      - if: ${{ matrix.version != 'latest' }}
        run: |-
          cosign \
              sign \
              --yes \
              ghcr.io/anza-labs/library/go-busybox@$(crane digest ghcr.io/anza-labs/library/go-busybox:${{ matrix.version }})
    strategy:
      matrix:
        target:
          - image
        version:
          - 0158b4566f6b5ea07d014d8f2b579458f213ae6b
name: release library/go-busybox
"on":
  push:
    branches:
      - main
    paths:
      - .github/workflows/gen__library_go-busybox.yaml
      - library/go-busybox/Dockerfile
      - library/go-busybox/pkg.toml
  workflow_dispatch: {}
permissions:
  contents: write
  id-token: write
  packages: write
