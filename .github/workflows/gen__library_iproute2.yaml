jobs:
  build:
    env: {}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          password: ${{ secrets.GITHUB_ACTION }}
          registry: ghcr.io
          username: shanduur-auto
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - run: |-
          docker \
              buildx \
              build \
              --load \
              --target=${{ matrix.target }} \
              --build-arg=VERSION=${{ matrix.version }} \
              --platform=linux/${{ matrix.arch }} \
              --file=library/iproute2/Dockerfile \
              --tag=ghcr.io/anza-labs/library/iproute2:${{ matrix.version }}-${{ matrix.arch }} \
              library/iproute2
      - run: |-
          docker \
              push \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }}-${{ matrix.arch }}
    strategy:
      matrix:
        arch:
          - amd64
          - arm64
          - ppc64le
          - riscv64
        target:
          - image
        version:
          - 6.15.0-r0
  promote:
    env: {}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          password: ${{ secrets.GITHUB_ACTION }}
          registry: ghcr.io
          username: shanduur-auto
      - uses: docker/setup-buildx-action@v3
      - run: |-
          docker \
              manifest \
              create \
              --amend \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }} \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }}-amd64 \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }}-arm64 \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }}-ppc64le \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }}-riscv64
      - run: |-
          docker \
              manifest \
              annotate \
              --arch=amd64 \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }} \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }}-amd64 && \
          docker \
              manifest \
              annotate \
              --arch=arm64 \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }} \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }}-arm64 && \
          docker \
              manifest \
              annotate \
              --arch=ppc64le \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }} \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }}-ppc64le && \
          docker \
              manifest \
              annotate \
              --arch=riscv64 \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }} \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }}-riscv64
      - run: |-
          docker \
              manifest \
              push \
              ghcr.io/anza-labs/library/iproute2:${{ matrix.version }}
    strategy:
      matrix:
        target:
          - image
        version:
          - 6.15.0-r0
name: release library/iproute2
"on":
  push:
    branches:
      - main
    paths:
      - .github/workflows/gen__library_iproute2.yaml
      - library/iproute2/Dockerfile
  workflow_dispatch: {}
permissions:
  contents: write
  packages: write
